/* 清理 WXSS 文件，移除大量自定义样式，适配新的 WXML 结构和全局样式 */

/* .container 已被全局样式替代 */
/* .loading-container 已移除 */

/* .section 已被全局 .card 替代 */
/* .section-title 保留基本样式，调整颜色和 margin */
.section-title {
  font-size: 30rpx;
  font-weight: 500;
  margin-bottom: 20rpx;
  color: #1d1d1f; /* 统一颜色 */
  display: flex;
  align-items: center;
}

.section-title::before {
  content: "";
  display: block;
  width: 8rpx;
  height: 28rpx;
  background-color: #0071e3; /* 改为全局主色 */
  margin-right: 12rpx;
  border-radius: 4rpx;
}

/* 移除旧的 .form-row, .form-label, .form-input */

/* Picker 样式 */
.picker-override {
  display: flex; 
  align-items: center;
  justify-content: space-between;
  padding-right: 20rpx; 
  box-sizing: border-box; 
}

.picker-override.picker-placeholder {
  color: #999;
}

.arrow {
  width: 16rpx;
  height: 16rpx;
  border-right: 3rpx solid #ccc;
  border-bottom: 3rpx solid #ccc;
  transform: rotate(45deg);
  margin-left: 10rpx;
}

/* 工厂和工序同行布局 */
.factory-process-row {
  display: flex;
  justify-content: space-between;
  gap: 32rpx;
}

.factory-col {
  flex: 1;
}

.process-col {
  flex: 1;
}

/* 产品列表样式 */
.product-list {
  margin-top: 20rpx;
}

.product-item {
  background-color: #ffffff;
  border-radius: 16rpx;
  margin-bottom: 20rpx;
  padding: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  overflow: hidden;
}

.product-info {
  display: flex;
  margin-bottom: 16rpx;
}

.product-image {
  width: 120rpx;
  height: 120rpx;
  border-radius: 8rpx;
  background-color: #f5f5f7;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.product-details {
  flex: 1;
}

.product-name {
  font-size: 24rpx;
  color: #86868b;
  margin-bottom: 4rpx;
}

.product-code {
  font-size: 34rpx;
  font-weight: 600;
  color: #1d1d1f;
  margin-bottom: 8rpx;
}

.product-tags {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  position: relative;
}

.product-actions-inline {
  margin-left: auto;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.product-tag {
  font-size: 26rpx;
  padding: 10rpx 20rpx;
  border-radius: 10rpx;
}

.tag-name {
  color: #86868b;
  margin-right: 4rpx;
}

.tag-value {
  color: #1d1d1f;
}

.product-controls {
  margin-top: 12rpx;
}

.quantity-weight {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-bottom: 16rpx;
}

.control-item {
  flex: 1; /* Allow items to share space equally in a row */
  display: flex;
  align-items: center;
  margin-right: 0; /* Remove individual margin, rely on parent's gap */
  min-width: 0; /* Allow items to shrink properly */
}

.control-label {
  font-size: 26rpx;
  color: #86868b;
  margin-right: 8rpx;
  white-space: nowrap;
}

.control-input {
  flex: 1;
  background-color: #eee;
  border-radius: 6rpx;
  padding: 0 15rpx;
  font-size: 26rpx;
  text-align: center;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 明细中的数值和单位容器 */
.control-value-container {
  display: flex;
  align-items: baseline;
  gap: 2rpx;
}

.control-value {
  font-size: 26rpx;
  color: #1d1d1f;
}

.control-unit {
  font-size: 20rpx;
  color: #8e8e93;
  font-weight: 400;
  margin-left: 2rpx;
}

.product-actions {
  display: flex;
  justify-content: flex-end;
  gap: 16rpx;
}

.btn-same-product.apple-btn {
  width: 28rpx;
  height: 28rpx;
  font-size: 18rpx;
  border-radius: 14rpx;
  background: #fff;
  color: #34C759;
  border: 2rpx solid #34C759;
  margin-left: 0;
  margin-right: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
}

.btn-small.btn-danger.apple-btn {
  width: 28rpx;
  height: 28rpx;
  font-size: 18rpx;
  border-radius: 14rpx;
  background: #fff;
  color: #ff3b30;
  border: 2rpx solid #ff3b30;
  margin-left: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
}

.btn-icon {
  display: none;
}

.btn-text {
  font-size: 32rpx;
  margin: 0;
  line-height: 1;
}

.btn-small {
  width: 48rpx;
  height: 48rpx;
  padding: 0;
  font-size: 32rpx;
  font-weight: bold;
  line-height: 42rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
}

.btn-danger {
  background-color: rgba(255, 59, 48, 0.1);
  color: #ff3b30;
}

.no-products {
  text-align: center;
  padding: 40rpx 0;
  color: #8e8e93;
  font-size: 28rpx;
}

/* === Product Confirm Modal Styles (REVISED Bottom Sheet - Ensure Full Width) === */
.product-confirm-modal .modal-content {
  width: 100% !important; /* Use !important to override conflicts */
  max-width: 100% !important;
  border-radius: 16rpx 16rpx 0 0; 
  position: fixed !important;
  bottom: 0 !important;
  left: 0 !important;
  top: auto !important;
  transform: translateY(100%); 
  transition: transform 0.3s ease-out;
  max-height: 85vh; 
  background-color: #ffffff; 
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.1); 
  z-index: 1102; /* Ensure it's above mask */
}

.product-confirm-modal.show .modal-content {
  transform: translateY(0); 
}

/* Mask should cover everything */
.product-confirm-modal .modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 1101; /* Below content, above page */
}

/* Unify Picker and Input Background and Height */
.product-edit-form .edit-form-value, /* Picker container */
.product-edit-form input /* All input fields */ {
  background-color: #f7f7fa !important; 
  border: 1rpx solid #f7f7fa; 
  height: 88rpx !important; /* Keep the increased height */
  border-radius: 10rpx;
  padding: 0 20rpx; 
  font-size: 28rpx;
  box-sizing: border-box;
  display: flex; 
  align-items: center; 
  width: 100%; 
}

/* Ensure picker text is left-aligned */
.product-edit-form .edit-form-value {
  justify-content: flex-start;
}

/* Style for the standalone price group and input */
.product-edit-form .price-group {
  margin-bottom: 20rpx; /* Maintain spacing */
}

.product-edit-form .full-width-input {
  width: 100% !important; /* Ensure full width */
  /* Inherit height, background, border etc. from the general input rule */
}

/* === Button Layout Adjustments === */
.product-confirm-modal .modal-actions {
  /* Keep existing padding, border-top, gap */
  /* Buttons are already stacked vertically by flex-direction: column */
}

/* Ensure confirm button is blue */
.product-confirm-modal .btn-primary {
  background-color: #007aff;
  color: #fff;
  border: none;
  order: 2;
}

/* Style the plus button */
.product-confirm-modal .btn-add-continue {
  width: 70rpx; /* Slightly larger */
  height: 70rpx;
  border-radius: 50%; /* Circle */
  background-color: #8e44ad;
  color: #fff; 
  border: 1rpx solid #8e44ad;
  box-shadow: 0 4px 12px rgba(142, 68, 173, 0.3);
  font-size: 45rpx; /* Adjust size */
  font-weight: 300;
  order: 1;
  /* display: flex, align-items, justify-content already applied */
}

/* Style the cancel button */
.product-confirm-modal .btn-default {
  background-color: #ffffff;
  color: #007aff;
  border: 1rpx solid #007aff; /* Match image style */
  order: 3;
}

/* === End Product Confirm Modal Styles === */

/* Remove or comment out old/conflicting styles below if necessary */
/* Example: 
.product-confirm-modal .some-old-style { display: none; } 
*/

/* 移除旧的总计样式 */
.total-amount-override {
  color: #ff3b30; /* 红色强调 */
  font-weight: 600;
}

/* 移除旧的 .submit-btn */
.submit-btn-override {
  margin-top: 40rpx;
  width: 100%;
}

/* 移除所有旧的模态框样式 */

/* --- 新的简化模态框样式 --- */
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1000;
  visibility: hidden;
  transition: visibility 0.3s;
}

.modal.show {
  visibility: visible;
}

.modal-mask {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  opacity: 0;
  transition: opacity 0.3s;
}

.modal.show .modal-mask {
  opacity: 1;
}

.modal-content-override {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  max-height: 85vh; /* 限制最大高度 */
  background-color: #ffffff;
  border-radius: 24rpx 24rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

.modal.show .modal-content-override {
  transform: translateY(0);
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx;
  border-bottom: 1rpx solid #f5f5f7;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1d1d1f;
}

.modal-close {
  font-size: 36rpx;
  color: #86868b;
  padding: 10rpx;
}

.modal-search {
  padding: 20rpx 12rpx; /* 增加垂直内边距 */
  border-bottom: 1rpx solid #e5e5ea;
}

.modal-body {
  flex: 1;
  overflow-y: auto;
  padding: 0 30rpx 30rpx; 
}

/* 货品选择列表 */
.product-select-list .list-item-override {
  margin-bottom: 16rpx;
  padding: 16rpx;
}

.list-item-content {
  display: flex;
  align-items: center;
}

.list-item-image {
  width: 80rpx;
  height: 80rpx;
  border-radius: 8rpx;
  margin-right: 20rpx;
  background-color: #f5f5f7;
}

.list-item-details {
  flex: 1;
}

.list-item-name {
  font-size: 26rpx;
  color: #86868b;
  margin-bottom: 4rpx;
}

.list-item-code {
  font-size: 28rpx;
  font-weight: 600;
  color: #1d1d1f;
  margin-bottom: 4rpx;
}

/* 确认添加弹窗 */
.selected-product-display {
  display: flex;
  align-items: center;
  padding: 20rpx;
  margin-bottom: 20rpx;
}

.product-edit-form .form-label {
  /* width: auto; */
  margin-bottom: 8rpx;
}

.product-edit-form .required::after { /* 沿用全局 required 样式? */
  /* content: '*'; 
  color: red;
  margin-left: 4rpx; */
}

.form-row-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx 24rpx;
  margin-bottom: 16rpx;
}

.form-row-grid .form-item {
  margin-bottom: 0;
}

.modal-actions-override {
  display: grid;
  grid-template-columns: 1fr 1fr; /* 两列按钮 */
  gap: 20rpx;
  margin-top: 30rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #f5f5f7;
}

.modal-actions-override .btn {
  width: 100%;
}

.modal-actions-override .btn-cancel-override {
  grid-column: span 2; /* 取消按钮占满一行 */
}

/* 苹果风全局背景 */
.page-bg {
  background: #f5f5f7;
  min-height: 100vh;
  padding: 24rpx;
}

/* 卡片样式 */
.card {
  background: #fff;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
  margin-bottom: 32rpx;
  padding: 32rpx;
}

/* 分区标题 */
.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1d1d1f;
  margin-bottom: 24rpx;
  letter-spacing: 1rpx;
}

/* 苹果风输入框 */
.apple-input {
  width: 100%;
  background: #f0f0f5;
  border-radius: 12rpx;
  padding: 20rpx; /* 保持或根据需要调整垂直内边距 */
  font-size: 28rpx;
  margin-bottom: 24rpx;
  border: none;
  box-sizing: border-box;
}

/* 苹果风按钮 */
.apple-btn {
  background: linear-gradient(90deg, #34C759 0%, #30B0FF 100%);
  color: #fff;
  border-radius: 24rpx;
  font-size: 32rpx;
  font-weight: 600;
  padding: 24rpx 0;
  box-shadow: 0 4rpx 16rpx rgba(52,199,89,0.08);
  margin-top: 16rpx;
  border: none;
}

.apple-btn:active {
  opacity: 0.85;
}

/* 优化表单、标签、分组等细节 */
.form-label, .form-value, .form-textarea {
  font-size: 28rpx;
  color: #333;
}

.form-label {
  font-weight: 500;
  margin-bottom: 8rpx;
}

.form-value {
  background: #f5f5f5;
  border-radius: 10rpx;
  padding: 20rpx;
  min-height: 40rpx;
}

.form-value.apple-input {
  background: #f0f0f5;
}

.form-textarea {
  min-height: 120rpx;
  border: none;
  background: #f0f0f5;
  border-radius: 12rpx;
  padding: 20rpx;
  font-size: 28rpx;
  margin-bottom: 24rpx;
}

/* 优化货品列表、操作按钮等 */
.product-item {
  border-radius: 14rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.03);
  margin-bottom: 20rpx;
  background: #fafbfc;
  padding: 20rpx;
}

.product-actions .apple-btn {
  min-width: 60rpx;
  padding: 0 16rpx;
  font-size: 28rpx;
  margin-left: 12rpx;
}

.add-product-btn.apple-btn {
  background: #007AFF;
  font-size: 30rpx;
  border-radius: 20rpx;
  margin-bottom: 0;
  margin-top: 0;
  box-shadow: 0 2rpx 8rpx rgba(0,122,255,0.08);
}

/* 优化弹窗 */
.product-modal .modal-content.card {
  border-radius: 18rpx;
  box-shadow: 0 4rpx 24rpx rgba(0,0,0,0.08);
  padding: 32rpx 24rpx;
}

.product-modal .apple-input {
  margin-bottom: 16rpx;
}

/* 其它细节优化可按需补充 */

/* 支付方式和金额 */
.payment-group {
  display: flex;
  justify-content: space-between;
  align-items: flex-end; /* 底部对齐 */
}

.payment-method-col {
  flex: 1;
  margin-right: 20rpx;
}

.payment-amount-col {
  width: 200rpx; /* 根据需要调整宽度 */
}

.payment-amount-input {
  text-align: right;
}

.picker-arrow {
  border: solid #ccc;
  border-width: 0 2rpx 2rpx 0;
  display: inline-block;
  padding: 5rpx;
  transform: rotate(45deg);
  margin-left: 10rpx;
  vertical-align: middle;
}

/* 勾选式支付方式 */
.payment-methods-row {
  display: flex;
  align-items: center;
}

.payment-label {
  flex-shrink: 0;
  margin-bottom: 0 !important;
  margin-right: 20rpx;
  font-size: 26rpx;
}

.payment-methods-container {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

/* 新增 single-line 样式 */
.payment-methods-container.single-line {
  flex-wrap: nowrap; /* 强制不换行 */
  overflow-x: auto; /* 如果内容超出则允许横向滚动 */
  padding-bottom: 8rpx; /* 为滚动条预留一点空间 */
  gap: 10rpx; /* 缩小间距 */
}

.payment-method-item {
  display: flex; 
  align-items: center;
  padding: 8rpx 12rpx;
  background-color: #f8f8fa;
  border-radius: 8rpx;
  border: 1px solid #e5e5ea;
  flex-shrink: 0; /* 防止项目被压缩 */
}

.payment-method-item.active {
  background-color: #ebf5ff;
  border-color: #4e86ff;
}

.payment-method-checkbox {
  width: 20rpx; /* 适当调整 checkbox 大小 */
  height: 20rpx;
  margin-right: 6rpx;
}

.checkbox-inner {
  width: 10rpx; /* 调整内部圆点大小 */
  height: 10rpx;
}

.payment-method-name {
  font-size: 22rpx; /* 确保名称字体也缩小 */
  color: #333;
}

/* 支付金额样式 */
.payment-amount-container {
  position: relative;
  margin-top: 20rpx;
}

.payment-amount-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.payment-input-wrapper {
  position: relative;
  flex: 1;
  margin-right: 20rpx;
  display: flex;
  align-items: center;
}

.payment-amount-input {
  padding-right: 100rpx;
  text-align: right;
  font-size: 30rpx;
  font-weight: 500;
  width: 100%;
  box-sizing: border-box;
  height: 120rpx; /* 与balance-info总高度匹配 */
  display: flex;
  align-items: center;
  justify-content: flex-end;
}

.payment-amount-label {
  position: absolute;
  bottom: 4rpx;
  right: 20rpx;
  font-size: 22rpx;
  color: #f04142;
  font-weight: 400;
}

.balance-info {
  min-width: 240rpx;
  font-size: 22rpx;
  color: #86868b;
  background-color: #f8f8fa;
  border-radius: 8rpx;
  padding: 12rpx 16rpx;
  height: 96rpx; /* 显式设置高度，不含边框 */
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.factory-balance,
.remain-balance {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 4rpx 0;
  line-height: 36rpx; /* 控制行高 */
}

.balance-label {
  color: #86868b;
  margin-right: 10rpx;
}

.balance-value {
  font-weight: 500;
  color: #333;
}

.balance-value.negative {
  color: #f04142;
}

/* 货品添加弹窗 */
.payment-amount-input-small {
  width: 180rpx;
  font-size: 26rpx;
  height: 70rpx;
  padding: 0 12rpx;
}

.order-amount-summary {
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-width: 180rpx;
  margin-left: 24rpx;
  gap: 8rpx;
}
.order-amount-item {
  display: flex;
  align-items: center;
}
.order-amount-label {
  color: #86868b;
  font-size: 24rpx;
  margin-right: 8rpx;
}
.order-amount-value {
  color: #1d1d1f;
  font-size: 28rpx;
  font-weight: 500;
}
.order-amount-value.negative {
  color: #f04142;
}

/* 微型按钮样式 */
.mini-btn {
  font-size: 24rpx;
  padding: 8rpx 16rpx;
  height: 60rpx;
  line-height: 44rpx;
  width: 160rpx; /* 添加固定宽度使按钮变短 */
  margin: 0 auto; /* 使按钮居中 */
}

/* 简化版按钮（无图标） */
.simple-btn {
  font-size: 28rpx;
  height: 70rpx;
  line-height: 70rpx;
  min-width: 160rpx;
  padding: 0 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 支付方式小标签 */
.small-label {
  font-size: 24rpx;
}

.small-method {
  font-size: 22rpx; /* 进一步缩小字体 */
  padding: 6rpx 10rpx; /* 调整内边距 */
  height: 46rpx; /* 调整高度 */
  line-height: 34rpx;
}

.payment-amount-input-small {
  font-size: 28rpx;
  height: 70rpx;
  line-height: 70rpx;
}

/* 调整支付模块样式 */
.payment-methods-row {
  margin-bottom: 20rpx;
}

/* 订单元数据样式调整 */
.order-meta {
  margin-top: 24rpx;
  margin-bottom: 24rpx;
  border-top: 1px solid #f0f0f0;
  padding-top: 24rpx;
}

.order-meta-row {
  display: flex;
  flex-direction: row;
  justify-content: flex-start;
  align-items: center;
}

.order-meta-item {
  display: flex;
  align-items: center;
  font-size: 22rpx;
  margin-right: 20rpx;
}

.meta-label {
  color: #6e6e73;
  font-weight: 500;
  margin-right: 8rpx;
  font-size: 22rpx;
}

.meta-value {
  color: #1d1d1f;
  font-weight: 400;
  font-size: 22rpx;
}

.payment-methods-container {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

/* 工厂和工序信息样式 */
.form-value {
  height: 80rpx;
  display: flex;
  align-items: center;
  padding: 0 20rpx;
  background-color: #ffffff;
  border-radius: 10rpx;
  font-size: 28rpx;
}

.form-value.placeholder {
  color: #999999;
}

.form-label.required:after {
  content: '*';
  color: #ff3b30;
  margin-left: 4rpx;
}

/* 添加分享相关样式 */
.modal-dialog {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 85%;
  max-width: 650rpx;
  background-color: #ffffff;
  border-radius: 16rpx;
  z-index: 1001;
  overflow: hidden;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.2);
}

.share-dialog .modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 32rpx;
  border-bottom: 1px solid #e5e5ea;
}

.share-dialog .modal-header text {
  font-size: 32rpx;
  font-weight: 500;
  color: #1d1d1f;
}

.share-dialog .close-btn {
  font-size: 40rpx;
  color: #86868b;
  line-height: 1;
}

.share-dialog .modal-content {
  padding: 24rpx 32rpx;
  max-height: 70vh;
  overflow-y: auto;
}

.share-preview {
  display: flex;
  justify-content: center;
  margin-bottom: 20rpx;
  overflow: hidden;
  border-radius: 12rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

.share-image {
  width: 100%;
  max-height: 800rpx;
}

.share-tips {
  text-align: center;
  font-size: 26rpx;
  color: #86868b;
  margin: 16rpx 0;
}

.share-dialog .modal-footer {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  padding: 24rpx 32rpx;
  border-top: 1px solid #e5e5ea;
  flex-wrap: wrap;
  gap: 16rpx;
}

.share-dialog .btn {
  width: 30%;
  height: 88rpx;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 500;
}

.share-dialog .cancel-btn {
  background-color: #f5f5f7;
  color: #1d1d1f;
}

.share-dialog .save-btn {
  background-color: #34c759;
  color: #ffffff;
}

.share-dialog .confirm-btn {
  background-color: #0071e3;
  color: #ffffff;
}

.preventTouchMove {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

/* 产品选择弹窗样式优化 */
.product-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease, visibility 0.3s ease;
  z-index: 1000;
}

.product-modal.show {
  opacity: 1;
  visibility: visible;
}

.modal-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 1001;
}

.modal-content {
  position: relative;
  width: 90%;
  max-width: 650rpx;
  max-height: 80vh;
  background-color: #fff;
  border-radius: 16rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  z-index: 1002;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
}

.modal-header {
  padding: 30rpx;
  border-bottom: 1px solid #e5e5ea;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.modal-title {
  font-size: 36rpx;
  font-weight: 500;
  color: #000;
}

.modal-close {
  font-size: 40rpx;
  color: #8e8e93;
  cursor: pointer;
}

.modal-search {
  padding: 20rpx 30rpx; /* 调整内边距使其与列表项对齐 */
  border-bottom: 1rpx solid #e5e5ea;
}

/* 新增：为产品选择弹窗内的搜索输入框 apple-input 设置样式 */
.product-modal .modal-search .apple-input {
  height: 80rpx; /* 设置一个合适的高度 */
  line-height: 80rpx; /* 垂直居中文本 */
  padding: 0 20rpx; /* 根据需要调整水平内边距 */
  /* 可以根据需要覆盖其他 apple-input 的样式 */
  background-color: #f2f2f7; /* 确保背景色 */
  border-radius: 10rpx; /* 确保圆角 */
}

.search-input-container {
  display: flex;
  align-items: center;
  background-color: #f2f2f7;
  border-radius: 10rpx;
  padding: 10rpx 20rpx;
}

.search-icon {
  margin-right: 10rpx;
  color: #8e8e93;
}

.search-input {
  flex: 1;
  height: 80rpx; /* 设置一个合适的高度 */
  line-height: 80rpx; /* 垂直居中文本 */
  padding: 0 20rpx; /* 根据需要调整水平内边距 */
  font-size: 28rpx;
  background-color: transparent;
}

.modal-body {
  flex: 1;
  max-height: 60vh;
  padding: 10rpx 0;
  overflow-y: auto;
}

.product-select-list {
  padding: 0 30rpx;
}

.product-select-list .card {
  margin: 20rpx 0;
  padding: 20rpx;
  border-radius: 12rpx;
  background-color: #fff;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
  border: 1px solid #e5e5ea;
}

.list-item-content {
  display: flex;
  align-items: center;
}

.list-item-image {
  width: 120rpx;
  height: 120rpx;
  border-radius: 8rpx;
  background-color: #f2f2f7;
  margin-right: 20rpx;
}

.list-item-details {
  flex: 1;
}

.list-item-code {
  font-size: 28rpx;
  color: #8e8e93;
  margin-bottom: 8rpx;
}

.list-item-name {
  font-size: 32rpx;
  font-weight: 500;
  color: #000;
}

.empty-search {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 0;
}

.empty-text {
  font-size: 30rpx;
  color: #8e8e93;
  text-align: center;
}

/* 产品确认弹窗样式优化 */
.product-confirm-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease, visibility 0.3s ease;
  z-index: 1100;
}

.product-confirm-modal.show {
  opacity: 1;
  visibility: visible;
}

.product-confirm-modal .modal-content {
  width: 90%;
  max-width: 650rpx;
  max-height: 85vh;
}

.selected-product {
  display: flex;
  align-items: center;
  padding: 30rpx;
  border-bottom: 1px solid #e5e5ea;
}

.selected-product .product-image {
  width: 120rpx;
  height: 120rpx;
  border-radius: 8rpx;
  margin-right: 20rpx;
  background-color: #f2f2f7;
}

.selected-product .product-details {
  flex: 1;
}

.selected-product .product-code {
  font-size: 28rpx;
  color: #8e8e93;
  margin-bottom: 8rpx;
}

.selected-product .product-name {
  font-size: 32rpx;
  font-weight: 500;
  color: #000;
}

.product-edit-form {
  padding: 20rpx 30rpx;
}

.form-row {
  display: flex;
  margin-bottom: 30rpx;
}

.form-col {
  flex: 1;
  margin-right: 20rpx;
}

.form-col:last-child {
  margin-right: 0;
}

.edit-form-label {
  display: block;
  font-size: 28rpx;
  color: #8e8e93;
  margin-bottom: 10rpx;
}

.edit-form-label.required::after {
  content: '*';
  color: #ff3b30;
  margin-left: 6rpx;
}

.edit-form-value {
  height: 80rpx;
  padding: 0 20rpx;
  border: 1px solid #e5e5ea;
  border-radius: 10rpx;
  font-size: 30rpx;
  display: flex;
  align-items: center;
  position: relative;
}

.edit-form-value.placeholder {
  color: #8e8e93;
}

.picker-arrow {
  position: absolute;
  right: 20rpx;
  width: 0;
  height: 0;
  border-left: 10rpx solid transparent;
  border-right: 10rpx solid transparent;
  border-top: 10rpx solid #8e8e93;
}

input.quantity-input, input.weight-input, input.price-input {
  height: 80rpx;
  padding: 0 20rpx;
  border: 1px solid #e5e5ea;
  border-radius: 10rpx;
  font-size: 30rpx;
}

/* 隐藏货品编辑弹窗中颜色和尺码选择的下拉箭头 */
.product-confirm-modal .product-edit-form .edit-form-value .picker-arrow {
  display: none;
}

.modal-actions {
  padding: 20rpx 30rpx 30rpx; /* Adjusted padding */
  display: flex; /* Use flex for button layout */
  flex-direction: column; /* Stack buttons vertically */
  align-items: center; /* Center items horizontally */
  gap: 16rpx; /* Space between buttons */
  border-top: 1rpx solid #f0f0f0; /* Separator line */
  margin-top: 10rpx;
}

.btn-add-continue {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background-color: #8e44ad;
  color: #fff;
  font-size: 50rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 30rpx;
  box-shadow: 0 4px 12px rgba(142, 68, 173, 0.3);
}

.full-width-btn {
  width: 100%;
  height: 90rpx;
  margin-bottom: 20rpx;
  border-radius: 10rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 30rpx;
}

button.btn-primary {
  background-color: #007aff;
  color: #fff;
  border: none;
}

button.btn-default {
  background-color: #ffffff;
  color: #007aff;
  border: 1rpx solid #007aff; /* Blue border */
}

.btn-hover {
  opacity: 0.8;
}

/* 工厂和工序标签样式 */
.form-label {
  font-size: 28rpx;
  color: #333333;
  margin-bottom: 12rpx;
  display: block;
}

.form-label.required:after {
  content: '*';
  color: #ff3b30;
  margin-left: 4rpx;
}

/* 支付方式标签 */
.payment-label.small-label {
  font-size: 20rpx; /* 进一步缩小标签字体 */
}

/* 支付方式选项 */
.small-method {
  font-size: 20rpx; /* 进一步缩小选项字体 */
  padding: 5rpx 8rpx; /* 微调内边距 */
  height: 42rpx; /* 调整高度 */
  line-height: 32rpx; /* 调整行高 */
}

.payment-method-name {
  font-size: 20rpx; /* 确保名称字体也缩小 */
  color: #333;
}

.payment-method-checkbox {
  width: 18rpx; /* 稍微缩小 checkbox */
  height: 18rpx;
  margin-right: 5rpx;
}

.checkbox-inner {
  width: 8rpx; /* 缩小内部圆点 */
  height: 8rpx;
}

.footer-actions {
  display: flex;
  justify-content: space-between;
  margin-top: 40rpx;
  padding: 0 20rpx 40rpx;
}

.footer-actions button {
  min-width: 220rpx;
  margin: 0;
}

/* 基础信息卡片（仅在查看模式显示，精简版，横向排列） */
.info-simple-header {
  background: #f2f2f7; /* 苹果风格浅灰色背景 */
  border-radius: 12rpx;
  padding: 24rpx;
  margin-bottom: 32rpx;
}

.info-row-horizontal {
  display: flex;
  /* justify-content: space-between; */ /* 移除空间分散 */
  align-items: flex-start; /* 顶部对齐，确保标签和值顶部对齐 */
  gap: 150rpx; /* 增大列之间的间距，将工序往右移动到中线偏右位置 */
}

.info-item-col {
  display: flex; /* 保持flex布局 */
  flex-direction: column; /* 保持垂直排列 */
  /* flex: 1; */ /* 移除 flex: 1 让列宽根据内容自适应 */
  flex-shrink: 0; /* 防止被压缩 */
  /* 可以考虑给一个最小宽度 */
  min-width: 120rpx; /* 示例：给列一个最小宽度 */
  /* 根据需要调整文本对齐方式 */
  text-align: left; /* 默认左对齐 */
}

.info-label {
  font-size: 28rpx;
  color: #888;
  width: 120rpx;
  font-weight: 500;
}

.info-value {
  font-size: 30rpx;
  color: #222;
  font-weight: 600;
}

.quantity-input, .weight-input {
  height: 88rpx;
  display: flex;
  align-items: center;
  font-size: 32rpx;
}

/* 工厂账户状态样式 */
.factory-account-status {
  display: flex;
  align-items: center;
  margin-top: 12rpx;
  font-size: 18rpx; /* 将字体从28rpx调小18rpx */
}

.account-status-label {
  color: #86868b;
  font-size: 24rpx; /* 明确设置标签的字体大小 */
}

.account-status-value {
  margin-left: 8rpx;
  font-size: 24rpx; /* 明确设置值的字体大小 */
}

.balance-positive {
  color: #34c759;
  font-weight: 600;
}

.balance-negative {
  color: #ff3b30;
  font-weight: 600;
}

.balance-zero {
  color: #b0b0b8;
}

/* ========== 工厂搜索相关样式 ========== */
.factory-search-container {
  position: relative;
  display: flex;
  align-items: center;
}

.factory-search-container input {
  flex: 1;
  padding-right: 50rpx; /* 为搜索图标留空间 */
}

.search-icon {
  position: absolute;
  right: 15rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 28rpx;
  color: #666;
  padding: 5rpx;
}

.selected-factory-display {
  font-size: 24rpx;
  color: #3875FF;
  margin-top: 8rpx;
  padding: 8rpx 12rpx;
  background-color: rgba(56, 117, 255, 0.1);
  border-radius: 6rpx;
}

/* 工厂搜索弹窗 */
.factory-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 9999;
  display: none;
}

.factory-modal.show {
  display: flex;
  justify-content: center;
  align-items: center;
}

.factory-modal .modal-content {
  width: 90%;
  max-width: 600rpx;
  max-height: 80%;
  background-color: #fff;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.15);
}

.factory-modal .modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx;
  border-bottom: 1rpx solid #e5e5e5;
  background-color: #f9f9f9;
}

.factory-modal .modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.factory-modal .modal-close {
  font-size: 40rpx;
  color: #666;
  padding: 8rpx;
  line-height: 1;
}

.factory-modal .modal-search {
  padding: 24rpx 30rpx;
  border-bottom: 1rpx solid #e5e5e5;
}

.factory-modal .modal-search input {
  width: 100%;
  height: 72rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  border: 1rpx solid #e5e5e5;
  border-radius: 10rpx;
  background-color: #f9f9f9;
}

.factory-modal .modal-body {
  max-height: 400rpx;
  overflow-y: auto;
}

.factory-select-list {
  padding: 20rpx;
}

.factory-item {
  margin-bottom: 16rpx;
  border: 1rpx solid #e5e5e5;
  border-radius: 12rpx;
  overflow: hidden;
  transition: all 0.2s ease;
}

.factory-item:last-child {
  margin-bottom: 0;
}

.factory-item:active {
  background-color: #f0f0f0;
  transform: scale(0.98);
}

.factory-item .list-item-content {
  display: flex;
  align-items: center;
  padding: 24rpx;
  position: relative;
}

.factory-icon {
  font-size: 40rpx;
  margin-right: 20rpx;
  width: 50rpx;
  text-align: center;
}

.factory-item .list-item-details {
  flex: 1;
  overflow: hidden;
}

.factory-item .list-item-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 8rpx;
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}

.factory-item .list-item-info {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.factory-phone, .factory-address {
  font-size: 24rpx;
  color: #666;
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}

.select-check {
  position: absolute;
  right: 24rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 32rpx;
  color: #3875FF;
  font-weight: bold;
}

.empty-search {
  padding: 60rpx 30rpx;
  text-align: center;
}

.empty-text {
  font-size: 28rpx;
  color: #999;
}

/* ========== 苹果简约风工厂下拉搜索样式 ========== */
.factory-dropdown-container {
  position: relative;
  z-index: 100;
}

.factory-search-input-wrapper {
  position: relative;
  display: flex;
  align-items: center;
}

/* 工厂输入框特定样式 - 与工序框保持一致的高度 */
.factory-search-input-wrapper .apple-input {
  height: 80rpx;
  line-height: 80rpx;
  padding: 0 50rpx 0 20rpx;
  margin-bottom: 0;
  display: flex;
  align-items: center;
  min-height: 80rpx;
  background: #f5f5f5;
  border-radius: 10rpx;
}

.factory-search-input-wrapper .search-icon {
  position: absolute;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 28rpx;
  color: #8e8e93;
  pointer-events: none;
}

.factory-dropdown {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20px);
  border-radius: 16rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.12), 0 2rpx 8rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid rgba(0, 0, 0, 0.06);
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  transform: translateY(-8rpx);
  transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  z-index: 1000;
  margin-top: 8rpx;
}

.factory-dropdown.show {
  max-height: 400rpx;
  opacity: 1;
  transform: translateY(0);
}

.dropdown-scroll {
  max-height: 400rpx;
  padding: 8rpx 0;
}

.dropdown-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx 20rpx;
  color: #8e8e93;
}

.dropdown-empty .empty-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
  opacity: 0.6;
}

.dropdown-empty .empty-text {
  font-size: 24rpx;
  color: #8e8e93;
}

.factory-dropdown-list {
  padding: 0;
}

.factory-dropdown-item {
  display: flex;
  align-items: center;
  padding: 16rpx 20rpx;
  margin: 0 8rpx;
  border-radius: 12rpx;
  transition: all 0.2s ease;
  cursor: pointer;
  position: relative;
}

.factory-dropdown-item:hover {
  background: rgba(0, 122, 255, 0.08);
}

.factory-dropdown-item:active {
  background: rgba(0, 122, 255, 0.15);
  transform: scale(0.98);
}

.factory-dropdown-item.selected {
  background: rgba(0, 122, 255, 0.1);
}

.factory-dropdown-item.selected::before {
  content: '';
  position: absolute;
  left: 8rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 4rpx;
  height: 32rpx;
  background: #007aff;
  border-radius: 2rpx;
}

.factory-info {
  flex: 1;
  overflow: hidden;
}

.factory-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #1d1d1f;
  margin-bottom: 4rpx;
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}

.factory-details {
  display: flex;
  flex-direction: column;
  gap: 2rpx;
}

.factory-phone,
.factory-address {
  font-size: 22rpx;
  color: #8e8e93;
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}

.check-mark {
  font-size: 28rpx;
  color: #007aff;
  font-weight: 600;
  margin-left: 12rpx;
  opacity: 0;
  transform: scale(0.8);
  transition: all 0.2s ease;
}

.factory-dropdown-item.selected .check-mark {
  opacity: 1;
  transform: scale(1);
}

.selected-factory-display {
  font-size: 24rpx;
  color: #007aff;
  margin-top: 8rpx;
  padding: 8rpx 12rpx;
  background: rgba(0, 122, 255, 0.08);
  border-radius: 8rpx;
  border: 1rpx solid rgba(0, 122, 255, 0.2);
}

/* 响应式适配 */
@media (max-width: 750rpx) {
  .factory-dropdown {
    margin-left: -8rpx;
    margin-right: -8rpx;
    border-radius: 12rpx;
  }
  
  .factory-dropdown-item {
    margin: 0 4rpx;
    padding: 14rpx 16rpx;
  }
  
  .factory-name {
    font-size: 26rpx;
  }
  
  .factory-phone,
  .factory-address {
    font-size: 20rpx;
  }
}

/* 合计栏样式优化 */
.product-totals {
  display: flex;
  justify-content: space-between;
  padding: 16rpx 0;
  border-top: 1rpx solid #e5e5ea;
  margin-top: 16rpx;
  align-items: center;
}

.total-item {
  display: flex;
  align-items: baseline;
  gap: 2rpx;
  flex: 1;
  justify-content: center;
}

.total-item:first-child {
  justify-content: flex-start;
}

.total-item:last-child {
  justify-content: flex-end;
}

.total-label {
  font-size: 24rpx;
  color: #1d1d1f;
  font-weight: 500;
}

.total-value {
  font-size: 24rpx;
  color: #1d1d1f;
  font-weight: 600;
}

.total-unit {
  font-size: 20rpx;
  color: #8e8e93;
  font-weight: 400;
  margin-left: 2rpx;
}